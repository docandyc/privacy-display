# 显示器隐私保护系统 — 工程改进清单

> 基于对当前 PoC 代码的逐文件审查，本文档列出**真实存在**（而非泛泛建议）的改进点。
> 每条改进包含：**现状/问题 → 改动位置 → 具体方案 → 验证方法 → 优先级/工作量**。
> 优先级依据用户重点（交底书特性补齐）排序：P0 > P1 > P2。

---

## 0. 总览与优先级矩阵

| 编号 | 改进项 | 类别 | 价值 | 工作量 | 优先级 | 状态 |
|------|--------|------|------|--------|--------|------|
| A1 | HDR 亮度补偿（ICtCp+PQ） | 交底书特性 | 高 | 中 | **P0** | ✅ 已实现 |
| A2 | 黑帧防御 + 自动曝光攻击模型 | 交底书特性 | 高 | 中 | **P0** | ✅ 已实现 |
| A3 | 多显示器同步 | 交底书特性 | 中 | 中 | **P0** | ✅ 已实现 |
| A4 | 性能实测 | 交底书特性 | 高 | 低 | **P0** | ✅ 已实现 |
| B1 | 真实对抗噪声 + 消融实验 | 安全可信度 | 高 | 中 | P1 | ✅ 已实现 |
| B2 | 去混淆/重构网络攻击实验 | 安全可信度 | 高 | 中 | P1 | ✅ 已实现 |
| B3 | 视角差异化掩模 | 安全可信度 | 高 | 中 | P1 | ✅ 已实现 |
| B4 | 屏幕-相机通信式强攻击基线 | 安全可信度 | 高 | 低 | P1 | ✅ 已实现 |
| B5 | 在线 VLM 读屏攻击评测 | 安全可信度 | 高 | 低 | P1 | ✅ 已实现入口，live 需可联网重跑 |
| B6 | 真实手机/相机拍摄结果分析入口 | 安全可信度 | 高 | 低 | P1 | ✅ 已实现模板与分析器，待采集实拍 |
| C1 | 掩模取模偏置修复 | 实验严谨性 | 中 | 低 | P2 | ✅ 已实现 |
| C2 | 多样本语料 + 多 OCR 引擎 | 实验严谨性 | 高 | 中 | P2 | ✅ 已实现 |
| C3 | 投稿表格可复现汇总 | 实验严谨性 | 高 | 低 | P2 | ✅ 已实现 |
| C4 | 复现实验 manifest | 实验严谨性 | 高 | 低 | P2 | ✅ 已实现 |
| C5 | SSIM 感知指标 + 运动模糊 | 实验严谨性 | 中 | 低 | P2 | ✅ 已实现 |
| C6 | pickle→npz | 实验严谨性 | 低 | 低 | P2 | ✅ 已实现 |

> **核心洞察**：当前 PoC 的"隐私保护"实际上几乎全部由**随机点阵掩模分割**贡献，而交底书宣称的核心创新之一"对抗性噪声"在代码里是未训练网络/边缘启发式，贡献微弱（见 B1 消融）。这是文档最重要的诚实结论。

---

## 1. P0 — 交底书特性补齐（用户重点）

### A1. HDR 亮度补偿（ICtCp 软裁剪 + PQ/HLG）

**现状/问题**
交底书 4.3 节详细描述了 HDR 模式非线性补偿（PQ/ST.2084 空间、`L_k_comp=min(L_max, L_k·n)`、ICtCp 软裁剪保色相）。但当前 `subframe_composer.py` 只实现了 SDR 背光提升模型（γ=1），完全没有 HDR 路径。亮度提升导致的超色域问题（交底书提到用 ICtCp 软裁剪解决）也未处理。

**改动位置**
- 新建 `src/core/hdr_compensation.py`
- 修改 `src/core/subframe_composer.py`：`SubframeComposer` 增 `hdr_mode` 参数

**具体方案**
1. PQ (ST.2084) EOTF/OETF 编解码：`pq_encode(L_nits) → [0,1]`、`pq_decode`
2. RGB↔ICtCp 转换（经 LMS 空间）
3. 超色域软裁剪：在 ICtCp 空间保持 I（亮度）与色相角不变，仅压缩超界饱和度
4. HDR 子帧补偿：线性光空间中 `L_k·n`，峰值钳到 `L_max`（1000–4000 nits）

**验证方法**
- PQ 往返误差 `|pq_decode(pq_encode(L))−L| < 1e-3`
- 软裁剪后色相分量 ΔH≈0（仅饱和度变化）
- n=8 时 HDR 补偿后峰值仍在物理范围
- HDR 模式下按 `peak_nits/content_peak_nits` 做视觉积分，headroom 允许时可还原内容亮度

**优先级/工作量**：P0 / 中（纯数学，约 200 行）

---

### A2. 黑帧防御 + 自动曝光攻击模型

**现状/问题**
交底书 3.2.4 提"每第 n+1 个时隙插入全黑帧，使相机 AE 误判环境亮度→后续帧过曝"；5.4 步骤17 提"帧生成延迟时插黑帧应急保护"。当前 `subframe_composer.py` 只有反色帧（`compose_inversion_frame`），无黑帧；`timing_controller.py` 无应急黑帧逻辑；`camera_simulator.py` 无 AE 攻击建模，因此"黑帧使相机过曝"这一防御**完全没有被验证**。

**改动位置**
- `src/core/subframe_composer.py`：增 `compose_black_frame()`
- `src/core/timing_controller.py`：增"渲染超时→应急黑帧"状态
- `src/attack/camera_simulator.py`：增 `auto_exposure_attack()`，建模相机 AE 增益自适应

**具体方案**
1. 黑帧 = 全 0；在周期末按策略插入，时长 `α·Δt`
2. AE 模型：相机依据近 N 帧平均亮度调整增益 `gain = target_lum / observed_lum`，黑帧拉低 observed_lum → gain 上升 → 后续真实子帧过曝饱和
3. 对比黑帧/无黑帧两种情况下 AE 攻击后的 OCR

**验证方法**
- 插黑帧后 AE 增益显著上升，过曝帧 OCR 进一步下降
- 单测：黑帧像素全 0；AE 增益随黑帧占比单调上升

**优先级/工作量**：P0 / 中

---

### A3. 多显示器同步

**现状/问题**
交底书 4.4 节描述主从时钟同步（Master 广播 VBlank，±0.1ms 容差）与异构刷新率处理（240Hz+144Hz→720Hz 虚拟时钟）。原 PoC 当时未覆盖，`timing_controller.py` 只管单显示器。

**改动位置**
- 新建 `src/core/multi_display.py`

**具体方案**
1. `MultiDisplaySync`：一个 Master + 多个 Slave，Master VBlank 时间广播
2. 异构刷新率：求各刷新率 LCM 作虚拟时钟基准，各屏按 `虚拟时隙数/n` 节拍取子帧
3. 同步误差统计：各从屏切换时刻与主屏的偏差

**验证方法**
- 两台模拟屏（240/144Hz）同步误差 <±0.1ms
- 任意相机曝光窗口内两屏均呈碎片态（无完整帧泄露）

**优先级/工作量**：P0 / 中

---

### A4. 性能实测

**现状/问题**
交底书 6.4 给出一张性能表（GPU 额外负载 3-5%、帧生成 +0.3ms、端到端 +0.5ms、CPU<1%），但这些数字在代码里**从未被测量**，纯属声称。答辩时若被问"这些数据怎么来的"会很被动。

**改动位置**
- 新建 `experiments/performance_benchmark.py`

**具体方案**
测量并如实报告：
1. 掩模生成耗时（`MaskGenerator.generate`，含拒绝采样后）
2. GPU 单子帧渲染耗时（`GPURenderer.render_subframe`）vs 软件渲染
3. 噪声生成耗时
4. 端到端单帧合成延迟、可达 FPS
5. `TimingController` 实测抖动（诚实说明 Python+非实时 OS 的局限）

**验证方法**
- 输出实测性能表，与交底书声称值并列对比，**标注差距与原因**（Python/CPU 渲染 vs 交底书假设的驱动层 GPU）

**优先级/工作量**：P0 / 低（最高性价比）

---

## 2. P1 — 安全可信度

### B1. 真实对抗噪声 + 消融实验

**现状/问题**（严重）
`noise_injector.py:96-128` 的 `generate_pytorch_fgsm` 用 `models.vgg11(weights=None)` —— **随机初始化、未训练**的网络，其梯度是无意义噪声，不构成任何"对抗攻击"。主路径 `_gradient_based_noise`（行 65-94）只是 Sobel 边缘 + 随机扰动的启发式。因此交底书"创新点 B：基于梯度攻击的对抗噪声"在实现层面**未被验证**。攻击分析已暗示真正起作用的是掩模分割，不是噪声。

**改动位置**
- `src/core/noise_injector.py`：`generate_pytorch_fgsm` 改用预训练权重
- 新建 `experiments/ablation_noise.py`：噪声消融

**具体方案**
1. 预训练模型路径：`weights=VGG11_Weights.DEFAULT`，损失改为对真实视觉特征的有界扰动（或攻击一个预训练 CRNN 文本识别代理）
2. 消融实验：四组对照
   - 仅掩模（无噪声）
   - 仅噪声（无掩模分割）
   - 掩模+启发式噪声（当前）
   - 掩模+真实对抗噪声
   测各组单帧 OCR，量化噪声边际贡献

**验证方法**
- 消融表显示：去掉噪声后单帧 OCR 变化 < X%（预期很小，证明掩模是主力）—— 这是诚实且有价值的结论

**优先级/工作量**：P1 / 中

---

### B2. 去混淆/重构网络攻击实验

**现状/问题**
交底书 7.3 把"AI 自适应去混淆攻击"列为重点防御对象，但实验里**完全没有这种攻击**。`camera_simulator.py` 只有时域平均（已证明能破解）。缺少对"攻击者训练重构网络"的量化。

**改动位置**
- 新建 `src/attack/reconstruction_attack.py`

**具体方案**
实现三档重构攻击，由弱到强：
1. 多帧中值堆叠 + 去噪（无需训练）
2. 单帧 inpainting 重构基线（OpenCV inpaint / 简单插值）
3. （可选）轻量 U-Net 学习"单子帧→原图"映射

**验证方法**
- 量化各档重构后的 OCR 恢复率，诚实呈现哪些攻击有效、哪些无效

**优先级/工作量**：P1 / 中

---

### B3. 视角差异化掩模（抗时域平均缓解）

**现状/问题**
这是 PoC 暴露的根本局限（多帧叠加可还原）的**唯一可行缓解方向**（交底书 7.2）。当前 `mask_generator.py` 全屏用同一掩模序列，任何对齐的相机都能完整采样。

**改动位置**
- `src/core/mask_generator.py`：增视角相关掩模

**具体方案**
1. 空间分区，不同区域用不同子密钥派生掩模序列
2. 建模离轴相机：因 LCD 视角依赖 + 区域掩模差异，离轴相机看到的子帧序列与正视不一致 → 帧对齐失败 → 时域平均无法重构
3. 对比正视 vs 离轴相机的时域平均还原质量

**验证方法**
- 模拟两个视角，离轴视角时域平均后 OCR 显著低于正视

**优先级/工作量**：P1 / 中

---

### B4. 屏幕-相机通信式强攻击基线

**现状/问题**
前一版攻击实验覆盖了全局快门、卷帘快门、时域平均、长曝光和重构/inpainting，但还缺少借鉴 DeepLight/Revelio/BRIGHTNESS 这类屏幕-相机通信工作的强攻击者：攻击者知道屏幕存在时域调制，会从连续视频里搜索周期相位、累加差分，并检查单个颜色通道是否形成侧信道。

**改动位置**
- `src/attack/camera_simulator.py`
- `src/evaluation/benchmark.py`
- `experiments/attack_analysis.py`
- `tests/test_corpus.py`
- `tests/test_noise_camera.py`

**具体方案**
1. `phase_search_recovery_attack()`：攻击者不知道周期起点，但知道窗口长度，枚举连续窗口并按覆盖率、对比、椒盐边缘和饱和度选择候选重构。
2. `weighted_differential_accumulator_attack()`：累加相邻帧差分，模拟从人眼不明显的时域变化中恢复相机侧信号。
3. `channel_selective_recovery_attack()`：只使用 red/green/blue/luma 单通道重构，检测颜色侧信道，特别是蓝通道。
4. `screen_camera_attack_suite()`：统一输出 `phase_search_mean`、`phase_search_max`、`differential_luma`、`differential_blue`、`blue_channel_max`，并附带 offset/channel/score 元数据。
5. `run_corpus_strong_camera_attacks()`：在 120 样本文本语料上运行单帧、完整周期平均、相位搜索、差分累加和单通道重构，输出均值、bootstrap CI、exact-match、敏感 token 恢复率和逐样本最强攻击。
6. `attack_analysis.py` 保存 `experiments/results/attack_analysis_strong_camera.json` 与 `experiments/results/corpus_strong_camera_attack.json`，用于论文表格复现。

**验证方法**
- `./.venv/bin/pytest tests/test_noise_camera.py -q`
- `./.venv/bin/pytest tests/ -q`
- `./.venv/bin/python experiments/attack_analysis.py`

**已观察结果**
在默认 `n=4, ε=8/255, 240Hz` 合成文本样本上，相位搜索完整周期重构、相位 max 和蓝通道 max 均恢复 OCR 至 100%；luma/blue 加权差分累加在当前样本上仍为 0%。全量 120 样本文本语料进一步确认：完整周期时域平均、相位搜索均值/最大值和蓝通道 max 的字符恢复率分别为 94.3%、94.2%、94.3%、95.0%，逐样本最强攻击字符恢复率为 95.2%、exact-match 为 91.7%、敏感 token 恢复率为 98.6%，泄露率（字符准确率≥20%）为 100%。这说明论文必须把“主动连续录制 + 完整周期重构/相位搜索/单通道重构”列为边界攻击，而不是只讨论普通时域平均。

**优先级/工作量**：P1 / 低

---

### B5. 在线 VLM 读屏攻击评测

**现状/问题**
三引擎 OCR 已覆盖 Tesseract、EasyOCR、PaddleOCR，但 IEEE Access 审稿人很可能追问“手机视觉助手/VLM 是否还能读屏”。如果只报告传统 OCR，结论会显得偏窄。

**改动位置**
- `src/attack/vlm_evaluator.py`
- `src/evaluation/vlm_benchmark.py`
- `experiments/vlm_readability_analysis.py`
- `tests/test_vlm_evaluator.py`
- `tests/test_vlm_benchmark.py`

**具体方案**
1. 接入 SiliconFlow/OpenAI-compatible `chat/completions`，默认模型 `Qwen/Qwen3-VL-32B-Instruct`。
2. VLM 只接收攻击帧图像和“转写可见文字”的 JSON 指令，**不发送 ground truth**，避免不可读样本被模型照抄答案。
3. 本地复用 `text_recovery_metrics()`，统一计算字符准确率、词级准确率、exact-match、敏感 token 恢复率。
4. 默认按语料类别抽样 9 张、每张评估 8 类攻击帧，共 72 次在线调用；结果保存为 `experiments/results/vlm_qwen3_siliconflow.json`。
5. API key 只从 `SILICONFLOW_API_KEY` 环境变量读取，不写入源码、README、命令历史建议或 JSON 结果。

**验证方法**
- `./.venv/bin/pytest tests/test_vlm_evaluator.py tests/test_vlm_benchmark.py -q`
- `./.venv/bin/python experiments/vlm_readability_analysis.py --dry-run --samples-per-category 1`
- 正式在线运行前在 shell 中设置 `SILICONFLOW_API_KEY`，再执行 `./.venv/bin/python experiments/vlm_readability_analysis.py --samples-per-category 1`

**当前状态**
接口、抽样 benchmark、结果汇总和单元测试已完成；脚本可从被 Git 忽略的 `.env.local` 读取 `SILICONFLOW_API_KEY`。本次 live 尝试受沙箱 DNS/用量限制影响，`experiments/results/vlm_qwen3_siliconflow.json` 只记录全 API-error 诊断，`publication_summary` 会将其标记为 `all_calls_failed`，论文不能引用该 0.0% 指标。下一步需要在可联网环境中重新运行 `scripts/reproduce_all.sh --with-vlm-live`，再扩大样本量并补手机系统 OCR/更多 VLM 模型。

**优先级/工作量**：P1 / 低

---

### B6. 真实手机/相机拍摄结果分析入口

**现状/问题**
IEEE Access 审稿会要求真实设备证据。当前已有模拟相机、强攻击者和在线 VLM 入口，但还缺少将手机/智能眼镜拍摄图片导入并按统一 OCR 指标分析的可复现路径。

**改动位置**
- 新建 `src/evaluation/real_capture.py`
- 新建 `experiments/real_capture_analysis.py`
- 新建 `experiments/real_captures/metadata_template.json`

**具体方案**
1. `--init-template` 生成真实拍摄 metadata 模板，记录设备、拍摄模式、距离、角度、曝光、帧率、刷新率、n、epsilon 和 ground truth。
2. 用户将真实照片或视频抽帧放入 `experiments/real_captures/`，复制模板为 `metadata.json` 后运行分析脚本。
3. 输出 `experiments/results/real_capture_ocr.json` 与 `real_capture_ocr.md`，按 condition/device/engine 汇总字符恢复率、exact-match、敏感 token 恢复率和泄露率。
4. `publication_summary.py` 将真实拍摄结果作为可选 section；无实拍文件时明确标记 unavailable，不伪造数值。

**验证方法**
- `pytest tests/test_real_capture.py -q`
- `python experiments/real_capture_analysis.py --init-template`
- 真实采集后运行 `python experiments/real_capture_analysis.py --engines tesseract`

**当前状态**
模板、分析器、publication summary 接入和单元测试已完成；尚未导入真实手机/相机拍摄素材，因此论文仍不能引用真实设备数值。

**优先级/工作量**：P1 / 低

---

## 3. P2 — 实验严谨性

### C1. 掩模取模偏置修复

**现状/问题**
`mask_generator.py:54-56`：`raw[:num_pixels] % self.n`，`raw` 是 uint8（0-255）。当 n 不是 2 的幂（n∈{3,5,6,7}）时存在**系统性取模偏置**——例如 256 mod 3 = 1，值 0 比值 1、2 多出现一次，偏置约 1/256。卡方检验（行 59-64）只能在偏置极端时报警，且重试（行 83 偏移 cycle）改变不了分布形状。这违背交底书"密码学安全均匀掩模"的声称。

**改动位置**
- `src/core/mask_generator.py`：`_generate_index_matrix` 改拒绝采样

**具体方案**
读 ⌈log₂n⌉ 位组成整数，落在 `[n, 2^⌈log₂n⌉)` 则拒绝重采。消除偏置（n=2,4,8,16 无影响，n=3,5,6,7 受益）。

**验证方法**
- 多次生成，各 n 的卡方 p 值应近似 Uniform(0,1)（无系统偏置）

**优先级/工作量**：P2 / 低

---

### C2. 多样本语料 + 多 OCR 引擎

**现状/问题**
`benchmark.py` 与 `main.py` 只用 **1 张合成文本图**，统计样本量 n=1，无显著性。`OCREvaluator` 检测到 EasyOCR 但 benchmark 固定 `ocr_engine="tesseract"`，交底书引用的 EasyOCR/PaddleOCR/YOLOv8 都没真跑。

**改动位置**
- 新建 `experiments/build_corpus.py`（生成 10-20 张：纯文本/代码/表格/中英混排）
- `src/evaluation/benchmark.py`：支持多引擎，报告 mean±std

**具体方案**
- 语料覆盖不同字号、版式、语言
- 同时跑 Tesseract + EasyOCR，输出每引擎的 mean±std

**验证方法**
- benchmark 输出语料级统计表（均值±标准差），样本量 ≥10

**优先级/工作量**：P2 / 中

---

### C3. 投稿表格可复现汇总

**现状/问题**
OCR、强相机攻击、检测、离轴和 VLM 结果分别保存在多个 JSON 中，手工拷贝到论文表格容易出现数字不一致。IEEE Access 审稿时也需要能追溯每个表格数值来自哪个实验文件。

**改动位置**
- 新建 `src/evaluation/publication_summary.py`
- 新建 `experiments/publication_summary.py`

**具体方案**
1. 只读取现有机器可读结果，不重跑实验、不调用在线服务。
2. 汇总 OCR、多引擎恢复指标、强相机攻击、YOLOv8n、离轴攻击和可选 VLM 结果。
3. 输出 `experiments/results/publication_summary.json` 与 `publication_summary.md`，作为论文表格的单一核对入口。

**验证方法**
- 缺失 VLM/view 等可选结果时显式标记 unavailable。
- Markdown 百分比只从源 JSON 派生，不手工写死。

**优先级/工作量**：P2 / 低

---

### C4. 复现实验 manifest

**现状/问题**
投稿补充材料需要说明实验环境、依赖版本、复现命令和结果文件哈希。只有 README 命令还不够：无法证明当前表格对应哪批 JSON，也无法快速检查结果文件是否被替换。

**改动位置**
- 新建 `scripts/reproduce_all.sh`
- 新建 `src/evaluation/reproducibility_manifest.py`
- 新建 `experiments/reproducibility_manifest.py`

**具体方案**
1. 记录 Python、平台、关键包版本、Git commit/branch/dirty 状态。
2. 列出主要复现实验命令，包括 dry-run VLM 与 live VLM 命令。
3. 对主要结果文件和关键源码/脚本文件计算 SHA-256 与字节数。
4. 对在线 VLM 只记录所需环境变量名 `SILICONFLOW_API_KEY`，不记录任何 API key 值。
5. 提供 `scripts/reproduce_all.sh` 作为统一编排入口：默认执行测试、VLM dry-run、投稿汇总和 manifest；`--full-offline` 重跑重型离线实验；`--with-vlm-live` 才执行真实在线 VLM。
6. 支持本地忽略的 `.env.local`，仓库只提供 `.env.example` 占位模板，真实 API key 不进入源码、README、结果 JSON 或 manifest。

**验证方法**
- 单测断言生成 JSON 中不会出现环境里的密钥值。
- `bash -n scripts/reproduce_all.sh` 检查 shell 语法。
- 运行 `scripts/reproduce_all.sh --skip-tests` 验证默认非联网编排路径。
- 运行 `python experiments/reproducibility_manifest.py` 生成 `experiments/results/reproducibility_manifest.json`。

**优先级/工作量**：P2 / 低

---

### C5. SSIM 感知指标 + 运动清晰度

**现状/问题**
`metrics.py` 只有 ΔE（CIEDE2000）+ 自定义 NMI。审稿人对"视觉无损"通常期待 SSIM/MS-SSIM。交底书 6.3 提的"pursuit camera 运动模糊宽度增幅 <5%"也没实现。

**改动位置**
- `src/evaluation/metrics.py`：增 `compute_ssim`、`compute_motion_blur_width`

**具体方案**
1. SSIM（积分帧 vs 原图）
2. Pursuit-camera 运动模糊模拟：沿运动方向对 n 子帧加权积分，测 moving edge 模糊宽度

**验证方法**
- SSIM > 0.98（视觉无损）；运动模糊宽度增幅 <5%

**优先级/工作量**：P2 / 低

---

### C6. pickle → npz

**现状/问题**
`noise_injector.py:178-194` 用 `pickle` 存噪声模板，`pickle.load` 有不安全反序列化风险，且跨版本不稳。

**改动位置**
- `src/core/noise_injector.py`：`save_template`/`load_templates` 改 `np.savez`/`np.load`

**优先级/工作量**：P2 / 低

---

## 4. 实施顺序与里程碑

```
里程碑 1（P0 交底书特性）：A1 → A2 → A3 → A4   ← 用户重点，先做
里程碑 2（P1 安全可信度）：B1 → B2 → B3 → B4 → B5
里程碑 3（P2 实验严谨性）：C1 → C2 → C3 → C4 → C5 → C6
每个里程碑结束跑 pytest 回归。
```

## 5. 风险与取舍

| 改进 | 取舍/风险 |
|------|-----------|
| A1 HDR | 真实效果需 HDR 屏（峰值 1000+ nits）；PoC 仅在数值/SDR 屏上模拟验证 |
| A3 多屏 | PoC 是软件时序模拟，±0.1ms 是理论值；真实需硬件 VBlank 同步 |
| A4 性能 | 实测值会**低于**交底书声称（Python+CPU 渲染 vs 驱动层 GPU），需如实标注差距 |
| B1 对抗噪声 | 消融很可能证明噪声贡献小——这对专利是把双刃剑，但科学诚实更重要 |
| B3 视角掩模 | 分区掩模可能略降正视视觉质量，需测 ΔE 代价 |
| B4 强攻击基线 | 会暴露更多失守路径（相位搜索/蓝通道 max），但这正是投稿所需的边界诚实性 |
| B5 在线 VLM | 会产生 API 成本且结果可能暴露更多可读路径；必须记录模型版本、抽样范围和调用日期 |
| C2 多引擎 | EasyOCR 首次运行下载模型、较慢；PaddleOCR 依赖重，PoC 可选做 |
| C3 汇总表格 | 汇总器只读取已有 JSON，不替代重跑实验；表格更新前仍需确认源 JSON 是最新实验产物 |
| C4 复现 manifest | manifest 记录的是生成时刻的文件状态；重跑实验或修改源码后需重新生成，避免哈希失效 |

---

## 6. 实测结果（实现后回填）

### A4 性能实测（`experiments/performance_benchmark.py`）

| 环节 | 1080p | 1440p | 交底书声称 |
|------|-------|-------|-----------|
| 掩模生成（拒绝采样） | 15.3 ms | 27.4 ms | <0.1ms (Compute Shader) |
| 对抗噪声生成 | 103.9 ms | 197.0 ms | — |
| 子帧合成 SDR (×4) | 79.9 ms | 141.9 ms | — |
| 子帧合成 HDR (×4) | 786.9 ms | 1367.2 ms | — |
| GPU 渲染单子帧 | 31.6 ms | 55.0 ms | +0.3ms (n=4) |
| **端到端单周期** | **199.1 ms** | **366.3 ms** | — |
| 240Hz 周期预算 | 16.67ms ✗ 超预算 | 16.67ms ✗ | — |

> **诚实结论**：PoC 的 Python/CPU 路径比交底书声称的驱动层 GPU 实现慢 1-2 个数量级。
> 交底书的 <0.3ms 指标假设 Compute Shader 并行，需将算子下沉至 GPU 驱动/合成器层
> （第四章），属 PoC 范围外。PoC 价值在**算法正确性与防御有效性**，非生产级性能。

### B2 去混淆/重构攻击（`src/attack/reconstruction_attack.py`，n=4 含对抗噪声）

| 重构方法 | PSNR | SSIM | OCR 恢复 | 结论 |
|----------|------|------|----------|------|
| **逐像素 max 堆叠** | **30.4 dB** | **0.991** | **100%** | ✗ **完全攻破** |
| 时域平均+锐化 | 3.7 dB | 0.478 | 100% | ✗ 攻破 |
| 中值堆叠 | 1.1 dB | 0.051 | 38% | 部分 |
| 单帧 inpainting | 2.3 dB | 0.026 | 0% | ✓ 防御有效 |

> **关键发现（比原 PoC 结论更尖锐）**：由于每像素在一个周期内恰好被点亮一次，
> 攻击者对覆盖完整周期的多帧做**逐像素取最大值**即可近乎完美还原（SSIM 0.991，
> OCR 100%），且对抗噪声因被 max 选择性忽略而**完全失效**。这把"多帧时域平均"
> 这一局限进一步坐实为"任何覆盖完整周期的逐像素聚合（max/median/mean）都能反演
> 互斥完备掩模"。单帧 inpainting 因缺失 3/4 像素而失败——单帧防御依然成立。

### B1 对抗噪声消融（`experiments/ablation_noise.py`，n=4）

| 实验组 | 单帧 OCR | 说明 |
|--------|----------|------|
| 基线（原始帧） | 89.2% | — |
| 组1 仅掩模（无噪声） | **0.0%** | 掩模单独即完全防御 |
| 组2 仅噪声（无掩模分割，ε=8/255） | **89.2%** | 默认舒适预算下，噪声单独不足以打掉干净数字图像 OCR |
| 组3 掩模+Tesseract 对比度目标 FGSM | 0.0% | 默认场景已被掩模压到 OCR 地板 |
| 组4 掩模+多模型轮换 PGD | 0.0% | 默认场景已被掩模压到 OCR 地板 |
| **默认场景噪声边际贡献（组1−组3）** | **+0.0%** | 掩模地板效应遮蔽噪声贡献 |

弱掩模泄露压力测试（模拟面板串扰/相机去噪/残影，leak=0.75，增强 ε=96/255）：

| 压力场景 | 单帧 OCR | 说明 |
|----------|----------|------|
| 弱掩模单独 | **90.5%** | 被关闭像素仍有残留，掩模不再单独充分 |
| 弱掩模 + OCR 对比度目标噪声 | **44.6%** | OCR 降幅 **45.9pct** |

> **更新结论**：默认 n=4/ε=8/255 下，随机点阵掩模仍是主防御，并且会把
> 单帧 OCR 压到地板，导致噪声贡献不可观测；但在弱掩模泄露/在线增强预算的
> 压力场景中，OCR 对比度目标噪声能提供可测的边际防御。对论文/专利表述应
> 避免声称"默认噪声单独有效"，而应表述为"掩模主防御 + 对抗噪声在泄露或
> 自适应攻击上升时作为二级强化"。

### A3 多显示器同步（`src/core/multi_display.py`）
- 同刷新率双屏（240Hz）：VBlank 广播同步误差 max 0.05ms < 0.1ms ✓
- 异构（240+144Hz）：虚拟时钟 LCM=720Hz，靠无完整帧泄露保证

### G2 YOLOv8n 目标检测评测（`src/attack/detection_evaluator.py` + `experiments/detection_attack.py`）

使用 `ultralytics==8.4.65` 与 YOLOv8n 权重 `yolov8n.pt`，输入为 Ultralytics
包自带 `assets/bus.jpg`。原始帧 YOLO 检测结果作为 pseudo-reference（5 个目标框），
再对单子帧与完整周期时域平均帧重新跑 YOLO 并计算 P/R/F1/mAP50：

| 场景 | YOLO 参考框 | 检出框 | Precision | Recall | F1 | mAP50 |
|------|------------|--------|-----------|--------|----|-------|
| 单子帧 | 5 | 3 | 0.67 | 0.40 | 0.50 | **0.40** |
| 完整周期时域平均 | 5 | 5 | 1.00 | 1.00 | 1.00 | **1.00** |

> 该结果补齐了交底书中“YOLOv8 检测模型”未实测的缺口。PoC 实测没有复现
> “0.92→0.08”的声称值：单子帧确实显著降低检测召回，但完整周期平均仍可完全恢复。
> 论文/答辩应引用本实验值，并继续限定威胁模型为“单帧/短曝光抽帧”。
>
> **独立发现：检测防御弱于 OCR 防御。** 单子帧目标检测 mAP50=**0.40**（召回 0.40），
> 明显高于单子帧 OCR 的 **0.0%**。原因是目标检测依赖粗粒度的形状/颜色团块，对
> 1/n 稀疏点阵采样的鲁棒性强于细笔画文本——25% 像素仍足以让 YOLO 命中部分大目标。
> 这意味着本方案对"文本/细结构信息"的保护强于"物体级场景信息"，论文应区分两类
> 威胁分别表述，不应笼统宣称"对所有识别任务等效防御"。

### 第二轮缺口补齐（G3–G13）

| 缺口 | 实现位置 | 验证/结果 |
|------|----------|-----------|
| G3 离轴相机攻击 | `CameraSimulator.off_axis_temporal_average_attack` / `off_axis_correction` + `experiments/view_attack.py` | 正视/离轴朴素/强攻击者校正 **OCR 全为 100%**；离轴 SSIM 仅降 0.064（见下方诚实结论） |
| G4 空间-时间联合扰动 | `NoiseInjector.split_complementary_spatial` | 逐像素 ΣN_k=0，2×2 棋盘邻域和≈0 |
| G5 学习型去混淆攻击 | `train_tiny_unet_reconstructor` + `experiments/unet_reconstruction.py` | tiny U-Net 单子帧重构 OCR **0%**（SSIM 0.783）；inpaint 在干净子帧 OCR **100%**、带噪声 pedestal 子帧 OCR **0%**（见下方 pedestal 消融） |
| G6 真实可微 OCR 路径 | `NoiseInjector._easyocr_e2e_gradient` | EasyOCR recognizer 可用时标记 `easyocr_e2e`，失败自动回退 shadow/surrogate |
| G7 视觉疲劳三件套 | `core/fatigue_policy.py` | 静态升 240Hz、动态降 120Hz；4000K 降蓝；d<50cm 降补偿 |
| G8 带宽约束 | `compute_bandwidth_gbps` + `check_bandwidth_fit` | 4K@240Hz RGBA≈76.4Gbps，DP1.4 不足、DP2.0 可承载 |
| G9 置换哈希 | `TimingToken.permutation_hash` | `sha256(permutation)` 随 `set_permutation` 原子更新 |
| G10 HLG 编码 | `hlg_encode` / `hlg_decode` | 往返误差 <1e-6 |
| G11 ALS 环境光闭环 | `AmbientAdaptation` | 5000 lux 下维持 Weber≈3:1，背光 scale 单调上升 |
| G12 1024周期预生成 | `MaskGenerator.pregenerate` | 预生成掩模/置换与按需生成一致 |
| G13 配置持久化 | `PrivacyDisplayConfig` + `main.py window <config.json>` | JSON 保存/加载 n/ε/α/K/refresh/brightness_model 并接入窗口配置 |

#### G3 诚实结论：视角差异化掩模对全周期对齐攻击者无 OCR 级保护

`experiments/view_attack.py`（n=4，35°，3×3 区域）对三条路径同时测 SSIM 与 OCR：

| 重构路径 | SSIM | OCR 字符准确率 |
|----------|------|----------------|
| 正视完整周期平均 | 1.000 | **100%** |
| 离轴朴素捕获 | 0.936 | **100%** |
| 离轴 + 强攻击者校正（反向位移 + 逐区域增益归一化） | 0.926 | **100%** |

> **关键澄清（修正第一版只测 SSIM 的偏差）**：整周期时域平均时，每个区域无论使用
> 何种差异化掩模都各自满足 ΣM_k=1、各自还原回原图——**掩模差异化不参与防御**，对
> 全周期对齐攻击者无密码学保护。离轴 SSIM 仅下降 0.064，且**完全来自区域级衰减/
> 色偏/位移这类可逆物理畸变**：朴素离轴捕获的 OCR 就已经是 100%，根本不需要校正。
> 交底书 7.2 把视角差异化列为"抗多帧叠加"缓解方向，但在纯软件可逆模型下它不成立；
> 真正能提供保护的是**不可逆**的物理 LCD 视角响应（色偏/对比度随角非线性、且攻击者
> 难以反演），而这属 PoC 范围外，论文应据此限定表述。

#### G5 诚实结论：单帧 inpaint 防御依赖对抗噪声 pedestal

`experiments/unet_reconstruction.py` 以 OCR 复核单帧重构（holdout 文本，n=4）：

| 单帧重构方法 | SSIM | OCR 字符准确率 |
|--------------|------|----------------|
| tiny U-Net（学习攻击下界） | 0.783 | **0%** |
| inpaint @ 干净子帧（未激活=0） | 0.929 | **100%** |
| inpaint @ 带噪声 pedestal 子帧（未激活≈ε） | — | **0%** |

> **修正 B2 表的隐含矛盾**：B2 表给出单帧 inpaint SSIM=0.026/OCR 0%，本实验干净子帧
> 却得 SSIM 0.93。根因是 `reconstruct_inpaint_single` 用 `gray<1` 检测缺失像素——
> B2 的子帧带对抗噪声 pedestal=8，未激活像素≈8 使缺失检测失效→inpaint 空操作（稀疏
> 暗帧，SSIM 0.026）；干净子帧未激活=0 时 inpaint 能填洞，加之白底黑字 SSIM 被背景
> 主导，故 SSIM 偏高。**但 SSIM 不是隐私指标，OCR 才是**：pedestal 消融显示——干净
> 子帧单帧 inpaint 可把高对比文档 OCR 还原到 **100%**，而加上对抗噪声 pedestal 后
> 同一攻击 OCR 跌回 **0%**。这把对抗噪声从"边际贡献存疑"提升为"单帧防御对 inpainting
> 攻击鲁棒的关键支撑"，与 B1"掩模主防御 + 噪声二级强化"一致，且给出了噪声不可或缺的
> 具体攻击场景（单帧 inpainting）。tiny U-Net 在 CPU/少样本下弱于 inpaint，仅作学习
> 攻击存在性下界。

### C2 多样本多引擎评测（`experiments/build_corpus.py` + `benchmark.run_corpus_multi_engine`）

语料已从 12 张扩展到 120 张：12 类模板 × 10 个确定性变体，覆盖中/英/混合凭据/数字/代码/表格/段落/财务字段/URL token，并生成 `corpus_metadata.json`，支持按类别、语言、版式、字号分层统计。

120 样本三引擎全量复测（n=4）：

| OCR 引擎 | 样本数 | 原始帧准确率 | 单子帧准确率 |
|----------|-------:|-------------:|-------------:|
| Tesseract | 120 | **94.0% ± 9.1%** | **0.0% ± 0.1%** |
| EasyOCR | 120 | **94.1% ± 8.1%** | **0.0% ± 0.0%** |
| PaddleOCR | 120 | **94.9% ± 7.3%** | **0.0% ± 0.1%** |

分层统计摘要：

| 维度 | 计数 |
|------|------|
| 类别 | 9 类（short_secret/sentence/mixed_credentials/numbers/code/table/paragraph/financial/url_token） |
| 语言 | en=40, zh=30, symbol=30, mixed=10, code=10 |
| 版式 | single=70, multi=50 |

已完成 120 样本三引擎全量复测：Tesseract、EasyOCR、PaddleOCR 原始帧准确率分别为 **94.0%±9.1%**、**94.1%±8.1%**、**94.9%±7.3%**，单子帧准确率分别为 **0.0%±0.2%**、**0.0%±0.0%**、**0.0%±0.0%**。新增恢复指标显示三引擎单子帧的词级准确率、exact-match、敏感 token 恢复率均为 **0.0%**，说明防御效果不只依赖单一 OCR 引擎或单一字符准确率指标。在线 VLM 入口已补，但本次 live 文件不可引用；投稿前建议继续加入有效 VLM、TrOCR、手机系统 OCR、更多 VLM 模型与真实拍摄样本。

### C3 投稿表格可复现汇总（`experiments/publication_summary.py`）

新增 `src/evaluation/publication_summary.py` 与 `experiments/publication_summary.py`，读取现有机器可读结果文件并输出：

- `experiments/results/publication_summary.json`
- `experiments/results/publication_summary.md`

汇总内容包括三引擎 OCR 语料结果、强相机攻击结果、YOLOv8n 目标检测、离轴攻击，以及可选的在线 VLM 结果状态。当前输出会读取 `vlm_qwen3_siliconflow.json`，但若所有行都是 API 错误，会明确标记为 `all_calls_failed` 且不可引用；真实拍摄 section 仍因没有导入实拍素材而标记 unavailable。投稿表格更新前应先运行该脚本，使论文数字有单一可审计来源。

### C4 复现实验 manifest（`experiments/reproducibility_manifest.py`）

新增 `src/evaluation/reproducibility_manifest.py` 与 `experiments/reproducibility_manifest.py`，输出：

- `experiments/results/reproducibility_manifest.json`

清单记录 Python/平台/包版本、Git commit/branch/dirty 状态、主要复现实验命令，以及关键结果文件和源码文件的 SHA-256。在线 VLM 只记录 `SILICONFLOW_API_KEY` 这个环境变量名，不记录密钥值；`scripts/reproduce_all.sh` 作为统一编排入口，默认只执行测试、VLM dry-run、投稿汇总和 manifest，重型离线实验与真实在线 VLM 均需显式参数。脚本可加载本地 `.env.local`，但仓库只保留 `.env.example` 模板，真实密钥仍不提交。当前生成结果显示主要结果文件和源码/脚本文件已被纳入清单，`secret_values_recorded=false`。该文件适合作为 IEEE Access 补充材料或 Zenodo/GitHub 归档前的完整性说明。

### C5 SSIM（视觉无损）
- 积分帧 vs 原图 SSIM = **0.99+**（>0.98 视觉无损阈值），与 ΔE<1.0 互为印证

### 回归测试
- `pytest tests/`：**183 passed**（含 HDR 积分、亮度模型、离轴校正、强攻击基线、扩展语料生成、统计置信区间、敏感 token 恢复率、增量多引擎报告、在线 VLM 评测入口、全失败 VLM 结果防误引、真实拍摄分析入口、投稿汇总器与复现 manifest 测试）

---

*本文档随实现进度更新「状态」列与各项的实测结果。*
