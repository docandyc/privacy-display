# Fix Figure 4 Error-Bar Reporting

## Goal

Correct Figure 4 in the English manuscript so that its error bars are drawn
from the reported bootstrap interval bounds and are fully defined in the
caption.

## What I Already Know

- The English manuscript source is `paper/main.tex`.
- Figure 4 is generated by
  `privacy-display/experiments/paper_figures/fig_f4_attack_bar.py`.
- The source result records 2,000 percentile-bootstrap resamples with capture
  as the resampling unit.
- The current plotting helper converts each interval to one symmetric
  half-width even though percentile-bootstrap bounds can be asymmetric.
- The Chinese manuscript is outside this task.

## Requirements

- Plot lower and upper error magnitudes directly from each metric's stored
  `ci95.low` and `ci95.high` values.
- Define the error bars in the English Figure 4 caption.
- State the bootstrap method, resampling unit, and number of resamples.
- Preserve the existing warning that the pooled sample counts are unbalanced.
- Avoid describing these intervals as evidence of cross-device or
  cross-scenario generalization.

## Acceptance Criteria

- [x] Figure 4 uses asymmetric error arrays derived from the stored interval
      endpoints.
- [x] The English Figure 4 caption identifies capture-resampled 95% percentile
      bootstrap intervals with 2,000 resamples.
- [x] The generated Figure 4 PDF is refreshed in the English manuscript.
- [x] The English manuscript completes its checked-in build successfully.
- [x] The final LaTeX log has no undefined citations or references.

## Out of Scope

- Chinese manuscript changes.
- Reanalysis or replacement of the underlying OCR data.
- Changes to figures other than Figure 4.
- Claims of population-level, device-level, or scenario-level inference.

## Technical Notes

- Follow `.trellis/spec/guides/latex-paper-build-thinking-guide.md`.
- Verify the generated artifact path and modification time after regeneration.
- Use `paper/build.sh` or the manuscript's documented complete build path.
- `privacy-display/experiments` is a tracked symlink to
  `/Volumes/MacExtension/Projects/privacy-display/experiments`. The plotting
  source was corrected at that target, but the source change cannot appear in
  this repository's Git diff. Figure regeneration therefore used an explicit
  `FIG_OUT` override targeting this workspace's `paper/figures` directory.
- Verification completed with `latexmk -g -xelatex main.tex`; the final log has
  no undefined citations or references. Existing font, color, and submission
  placeholder warnings are unrelated to this task.
